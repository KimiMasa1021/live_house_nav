class FormValidation {
  static String? emailValidation(String? val) {
    if (val == null || val.isEmpty) return "入力してください";
    return null;
  }

  static String? passwordValidation(String? val) {
    if (val == null || val.isEmpty) return "入力してください";

    return null;
  }

  static String? nameValidation(String? val) {
    if (val == null || val.isEmpty) return "入力してください";

    return null;
  }
}
